import React from 'react';
import { SheetPage } from '../SheetPage';

export const BattingHistoryPage: React.FC = () => (
	<SheetPage
		sheetName="Batting History"
		title="Batting History"
		description="Match-by-match batting history for all players."
	/>
);
