import React from 'react';
import { SheetPage } from '../SheetPage';

export const PlayerListPage: React.FC = () => (
	<SheetPage
		sheetName="Player list"
		title="Player List"
		description="Full list of registered players."
	/>
);
